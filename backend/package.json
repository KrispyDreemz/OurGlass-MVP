{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "pretest": "if [ ! -f .env ]; then cp .env.example .env; fi",
    "test": "npm run db:setup && jest --runInBand",
    "db:setup": "DATABASE_URL=${DATABASE_URL:-file:./dev.db} prisma generate && prisma migrate deploy || echo 'Skipping DB migrations (Codex fallback)'",
    "postinstall": "npm run db:setup"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "express": "^5.1.0",
    "prisma": "^6.12.0"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "supertest": "^7.1.4"
  }
}
